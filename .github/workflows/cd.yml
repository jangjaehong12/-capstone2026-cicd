# CD 파이프라인: main 브랜치에 push 되면 자동 실행
name: CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: 소스코드 체크아웃
        uses: actions/checkout@v4

      # 1. JDK 17 설치 (Maven 빌드를 위해 필요)
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven' # Maven 의존성 캐싱으로 빌드 속도 향상

      # 2. Maven을 이용한 Java 프로젝트 빌드
      - name: Build with Maven
        run: mvn clean package -DskipTests
        working-directory: ./backend  # backend 폴더로 이동해서 실행

      # 3. Docker Hub 로그인
      - name: Docker Hub 로그인
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Backend Docker Build
        run: |
          docker build -t jaehongjang1/yj-backend:${{ github.sha }} ./backend
          docker tag jaehongjang1/yj-backend:${{ github.sha }} jaehongjang1/yj-backend:latest

      - name: Frontend Docker Build
        run: |
          docker build -t jaehongjang1/yj-frontend:${{ github.sha }} ./frontend
          docker tag jaehongjang1/yj-frontend:${{ github.sha }} jaehongjang1/yj-frontend:latest

      - name: Docker Hub Push
        run: |
          docker push jaehongjang1/yj-backend:${{ github.sha }}
          docker push jaehongjang1/yj-backend:latest
          docker push jaehongjang1/yj-frontend:${{ github.sha }}
          docker push jaehongjang1/yj-frontend:latest